version: 1
frontend:
  phases:
    preBuild:
      commands:
        # Lệnh cũ (bạn giữ nguyên)
        - echo "===================> Đang chạy amplify.yml ở git"
        - nvm install 20
        - npm install -g pnpm
        # Lệnh mới: Thêm console.log/echo
        # 1. In ra giá trị biến môi trường trong quá trình build
        - echo "Kiểm tra Biến Môi trường MONGODB_URI:"
        - echo $MONGODB_URI
        # 2. Hoặc chạy một đoạn mã Node.js nhỏ để gỡ lỗi:
        - node -e 'console.log("Phiên bản Node đang dùng:", process.version);'

        - pnpm install
    # ... (các phần còn lại)
